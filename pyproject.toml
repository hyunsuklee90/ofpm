[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ofpm"
version = "0.1.0"
description = "Offline package manager scaffold"
requires-python = ">=3.10"

[project.scripts]
ofpm = "ofpm.cli:main"
